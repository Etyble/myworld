---
import Layout from "../layouts/Layout.astro";
---

<Layout title="Plan — MyWorld" description="Big ideas worth building.">
  <section class="hero">
    <div class="hero-bg"></div>
    <div class="container hero-content">
      <p class="kicker">理想主义者的蓝图</p>
      <h1 class="title">
        The <span class="gradient-text">Plan</span>
      </h1>
      <p class="intro">
        这些想法在今天看来或许不切实际，但每一个改变世界的技术，<br />
        最初都只是一小群人的"疯狂"念头。
      </p>
    </div>
  </section>

  <section class="visions">
    <div class="container">

      <!-- Vision 1: Blockchain Labor Value -->
      <article class="vision-card" id="labor">
        <div class="vision-number">01</div>
        <h2>劳动价值记录体系</h2>
        <p class="vision-subtitle">基于区块链的绝对公平分配</p>
        <div class="vision-body">
          <p>
            现有的经济体系中，劳动价值的记录与分配充满了中间环节和不透明。
            我们要构建一个全新的劳动价值记录体系——
          </p>
          <ul>
            <li>每一份劳动的贡献被<strong>精确记录</strong>在链上，不可篡改、不可删除</li>
            <li>报酬分配由智能合约驱动，<strong>规则透明、执行自动</strong>，没有人可以干预</li>
            <li>区块链必须是<strong>绝对安全</strong>的——这不仅仅指密码学层面，更意味着共识机制本身经得起任何攻击向量的考验</li>
            <li>去中心化不等于无序，我们追求的是<strong>公平的、可验证的秩序</strong></li>
          </ul>
          <p>
            这不仅仅是技术问题，更是一个社会学实验。如果劳动价值可以被公平地衡量和回报，
            人类协作的方式将被彻底改变。
          </p>
        </div>
      </article>

      <!-- Vision 2: New AI Framework -->
      <article class="vision-card" id="ai">
        <div class="vision-number">02</div>
        <h2>打破数据驱动AI的底层框架</h2>
        <p class="vision-subtitle">让机器像人一样，从少量知识中快速掌握技能</p>
        <div class="vision-body">
          <p>
            当前的AI大模型本质上是<strong>统计拟合机器</strong>——它们需要吞噬海量数据、
            消耗天文数字的算力，才能获得人类用很少样本就能掌握的认知能力。
          </p>
          <ul>
            <li>人类可以从<strong>一个例子</strong>中抽象出规则，而今天的AI需要数百万个</li>
            <li>数据驱动的范式<strong>不可持续</strong>：能源消耗、数据版权、边际收益递减</li>
            <li>我们需要一套<strong>全新的底层框架</strong>——不仅仅是改进现有架构，而是从认知科学、
            神经符号推理、因果推断等领域重新出发</li>
            <li>目标：一个能用<strong>极少样本</strong>完成概念抽象、推理和迁移的智能系统</li>
          </ul>
          <p>
            人类不能在数据驱动AI这条路上停留太久——它的天花板已经若隐若现。
            新的范式不是可选项，是必然。
          </p>
        </div>
      </article>

      <!-- Vision 3: New Hardware -->
      <article class="vision-card" id="hardware">
        <div class="vision-number">03</div>
        <h2>超越冯·诺依曼的新硬件架构</h2>
        <p class="vision-subtitle">为下一代AI量身打造的底层计算平台</p>
        <div class="vision-body">
          <p>
            摩尔定律正在失效，我们撞上了物理的墙。但真正的问题不在于晶体管密度——
            而在于<strong>架构本身</strong>。
          </p>
          <ul>
            <li>冯·诺依曼架构将计算与存储分离，数据在两者之间反复搬运——<strong>这是瓶颈的根源</strong></li>
            <li>未来的AI硬件需要<strong>存算一体</strong>、<strong>近数据处理</strong>，
            甚至彻底重新定义"计算"的含义</li>
            <li>我们要探索的不仅仅是更快的芯片，而是<strong>与新型AI范式深度耦合的硬件</strong>——
            硬件和算法应该像生物大脑一样彼此塑造</li>
            <li>愿景：一个脱离冯·诺依曼束缚的计算平台，让AI在上面<strong>自然地生长和思考</strong></li>
          </ul>
          <p>
            这需要从半导体物理、电路设计、到编译器、到上层编程模型的全栈重构。
            跨度极大，但每一步都值得走。
          </p>
        </div>
      </article>

    </div>
  </section>

  <section class="closing">
    <div class="container">
      <p class="closing-text">
        三个方向，看似独立，实则彼此咬合——<br />
        新的价值体系提供协作基础，新的AI提供智能工具，新的硬件提供物理载体。<br />
        这是一个<strong>闭环</strong>。
      </p>
      <p class="closing-sub">
        疯狂是理想主义的另一个名字。
      </p>
    </div>
  </section>
</Layout>

<style>
  .hero {
    position: relative;
    padding: 120px 0 80px;
    overflow: hidden;
  }

  .hero-bg {
    position: absolute;
    inset: 0;
    background:
      radial-gradient(ellipse 60% 50% at 50% 30%, rgba(124, 58, 237, 0.1), transparent),
      radial-gradient(ellipse 50% 40% at 20% 70%, rgba(99, 102, 241, 0.06), transparent);
    pointer-events: none;
  }

  .hero-content {
    position: relative;
    text-align: center;
    animation: rise 1s ease-out;
  }

  @keyframes rise {
    from {
      opacity: 0;
      transform: translateY(24px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  .kicker {
    font-size: 0.85rem;
    text-transform: uppercase;
    letter-spacing: 0.2em;
    color: var(--accent);
    margin-bottom: 16px;
  }

  .title {
    font-size: clamp(2.5rem, 7vw, 4.5rem);
    font-weight: 700;
    margin-bottom: 24px;
  }

  .gradient-text {
    background: linear-gradient(135deg, #a78bfa 0%, #7c3aed 40%, #6366f1 70%, #a78bfa 100%);
    background-size: 200% 200%;
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
    animation: gradient-shift 6s ease-in-out infinite;
  }

  @keyframes gradient-shift {
    0%, 100% { background-position: 0% 50%; }
    50% { background-position: 100% 50%; }
  }

  .intro {
    color: var(--text-muted);
    font-size: 1.05rem;
    line-height: 1.8;
    max-width: 560px;
    margin: 0 auto;
  }

  /* ---- Vision cards ---- */
  .visions {
    padding: 0 0 80px;
  }

  .vision-card {
    position: relative;
    background: var(--surface);
    border: 1px solid var(--border);
    border-radius: var(--radius);
    padding: 48px;
    margin-bottom: 32px;
    transition: border-color 0.4s, box-shadow 0.4s;
    animation: fade-up 0.6s ease-out both;
    animation-timeline: view();
    animation-range: entry 20%;
  }

  @keyframes fade-up {
    from {
      opacity: 0;
      transform: translateY(32px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  .vision-card:hover {
    border-color: var(--accent-glow);
    box-shadow: 0 0 40px rgba(124, 58, 237, 0.08);
  }

  .vision-number {
    font-size: 0.8rem;
    font-weight: 600;
    letter-spacing: 0.1em;
    color: var(--accent);
    margin-bottom: 12px;
  }

  .vision-card h2 {
    font-size: 1.6rem;
    margin-bottom: 8px;
  }

  .vision-subtitle {
    color: var(--text-muted);
    font-size: 1rem;
    margin-bottom: 24px;
    padding-bottom: 24px;
    border-bottom: 1px solid var(--border);
  }

  .vision-body p {
    color: var(--text-muted);
    font-size: 0.95rem;
    line-height: 1.8;
    margin-bottom: 16px;
  }

  .vision-body ul {
    list-style: none;
    padding: 0;
    margin: 0 0 20px;
  }

  .vision-body li {
    position: relative;
    padding-left: 24px;
    margin-bottom: 10px;
    color: var(--text-muted);
    font-size: 0.93rem;
    line-height: 1.7;
  }

  .vision-body li::before {
    content: "→";
    position: absolute;
    left: 0;
    color: var(--accent);
  }

  .vision-body strong {
    color: var(--text);
    font-weight: 600;
  }

  /* ---- Closing ---- */
  .closing {
    padding: 80px 0 120px;
    text-align: center;
  }

  .closing-text {
    font-size: 1.1rem;
    line-height: 2;
    color: var(--text);
    margin-bottom: 24px;
  }

  .closing-sub {
    font-size: 1.6rem;
    font-weight: 600;
    background: linear-gradient(135deg, #a78bfa, #6366f1);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
  }
</style>
